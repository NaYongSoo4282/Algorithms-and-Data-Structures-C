#설계결정
1. 데이터는 정수로만 받는다.
    우선 간단한 케이스를 구현해보기 위함. 
2. 데이터 중복은 없다.
    데이터 중복을 다루는 케이스로 변모할 예정
3. 단일연결리스트
    이중연결리스트는 다른 버전으로 구현 예정
#설계사항
1. Trailer 사용
    트레일러를 사용하지 않으면 발생하기 쉬움. (예외처리, 삽입과 삭제의 복잡함)
    트레일러를 사용해서 발생하는 비용은 sizeof(Node)